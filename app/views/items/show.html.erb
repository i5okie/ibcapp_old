<p id="notice"><%= notice %></p>

<div class="row">
	  <div class="large-10 large-offset-4 columns"><h2><%= @item.vendor %> <%= @item.name %></h2></div>
  <div class="row">
	  <div class="small-5 columns"><%= image_tag @item.photo.url(:medium) %></div>
	  <div class="small-6 columns"><%= @item.description %></div>
  </div><div class="row">
	  <div class="panel">Supplier: <span class="radius secondary label"><%= @item.source %></span> &emsp;&emsp;&emsp; Mfr. Part Number: <span class="radius secondary label"><%= @item.vpn %></span>   &emsp;&emsp;&emsp; Supplier Part Number: <span class="radius secondary label"><%= @item.sku %></span> &emsp;&emsp;&emsp; Cost: </strong><span class="radius secondary label">$ <%= @item.cost %></span></div>
  </div>
</div>


<%= link_to 'Back', items_path, class: "small radius button success" %> |
<%= link_to 'Edit', edit_item_path(@item), class: "small radius button alert" %>
